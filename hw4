#include <cstdlib>
#include <iostream>
#include <ctime>
#include <windows.h>
using namespace std;

int main() {
	setlocale(LC_ALL, "russian");

	//Задвние 1

	/*
	const int row = 3, column = 4;			
	int array[row][column];					
	int count = 0;

	for (int i = 0; i < row; i++)			
		for (int j = 0; j < column; j++)	
			array[i][j] = rand() % 11;		
	cout << " Получен массив \n";

	for (int i = 0; i < row; ++i) {			
		for (int j = 0; j < column; ++j)
			cout << array[i][j] << " ";
		cout << endl;						
	}


	for (int i = 0; i < row; i++)			
		for (int j = 0; j < column; j++)	
			if (array[i][j] == 0)
				count++;

	cout << " Количество элементов массива, равные нулю: " << count;
	*/




	//Задание 2(рисунок В)

	/*const int row = 5, column = 5;
	int array[row][column];
	int count = 0;
	int max;

	for (int i = 0; i < row; i++)
		for (int j = 0; j < column; j++)
			array[i][j] = rand() % 100;
	cout << " Получен массив 2: \n";

	for (int i = 0; i < row; ++i) {
		for (int j = 0; j < column; ++j)
			cout << " " << array[i][j] << " ";
		cout << endl;
	}

	cout << "\n";
	max = array[0][0];
	for (int i = 0; i < row; i++) 
		for (int j = 0; j < column; j++) {
			if (i == 0) {
				if (max < array[i][j]) max = array[i][j];
			}
			if ( i == 1 )
				if (j == 1 or j == 2 or j == 3) {
					if (max < array[i][j]) max = array[i][j];
				}
			if (i == 2 and j == 2) {
				if (max < array[i][j]) max = array[i][j];
			}
		}
	cout << " max = " << max;
	cout << "\n";
	*/

	return 0;
}
